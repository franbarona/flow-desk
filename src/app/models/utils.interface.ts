export interface DropdownOptions {
    id: string,
    name: string
}

export interface MultiselectDropdown {
  id: string;
  label: string;
  extraLabel?: string;
  avatarUrl?: string;
  color?: string;
}